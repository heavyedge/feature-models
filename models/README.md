# Models

# v0

- Trained on mean profile shape features.
- X : `Rgt`, `Ca`, `cos(theta)`.
- y : `H`, `b`.
- y are independent.

# v1

- Trained on all profile shape features.
- X : `Rgt`, `Ca`, `cos(theta)`.
- y : `H`, `b`.
- y are independent.
