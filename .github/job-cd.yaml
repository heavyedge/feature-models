apiVersion: batch/v1
kind: Job
metadata:
  name: ${JOB_NAME}
  namespace: ${KUBE_NAMESPACE}
  annotations:
    creator: "${KUBE_USERNAME}"
spec:
  ttlSecondsAfterFinished: 600
  activeDeadlineSeconds: 21600
  backoffLimit: 1
  template:
    metadata:
      labels:
        git-sha: ${GIT_SHA}
        github-event: ${EVENT_NAME}
      annotations:
        sidecar.istio.io/inject: "false"
        creator: "${KUBE_USERNAME}"
    spec:
      nodeSelector:
          mlx.navercorp.com/zone: ${MLXP_ZONE}
      restartPolicy: Never
      imagePullSecrets:
        - name: regcred
      volumes:
        - name: postfix-state
          emptyDir: {}
        - name: gmail-smtp-relay
          secret:
            secretName: gmail-smtp-relay
      containers:
        - name: build
          image: ${DOCKER_REGISTRY}/${DOCKER_USERNAME}/heavyedge/feature-models:${DOCKER_TAG}
          imagePullPolicy: Always
          resources:
            requests:
              memory: "4Gi"
              cpu: "${CPU_REQUEST}"
              nvidia.com/gpu: "${GPU_REQUEST}"
            limits:
              memory: "16Gi"
              cpu: "${CPU_REQUEST}"
              nvidia.com/gpu: "${GPU_REQUEST}"
          command: ["/bin/sh", "-c"]
          args:
            - |
              state_dir="${HEAVYEDGE_STATE_DIR:-/var/run/heavyedge}"
              state_file="${DEPLOY_STATE_FILE:-${state_dir}/deploy-state.json}"
              metadata_tmp_file="${DEPLOY_METADATA_TMP_FILE:-/tmp/deploy-metadata.json}"
              checks_tmp_file="${DEPLOY_CHECKS_TMP_FILE:-/tmp/deploy-checks.json}"
              heartbeat_file="${DEPLOY_BUILD_HEARTBEAT_FILE:-${state_dir}/build-heartbeat}"
              heartbeat_interval="${DEPLOY_BUILD_HEARTBEAT_INTERVAL_SECONDS:-5}"

              mkdir -p "$(dirname "${state_file}")"
              mkdir -p "$(dirname "${heartbeat_file}")"

              write_heartbeat() {
                heartbeat_tmp_file="${heartbeat_file}.$$"
                date +%s > "${heartbeat_tmp_file}"
                mv "${heartbeat_tmp_file}" "${heartbeat_file}"
              }

              (
                while :; do
                  write_heartbeat
                  sleep "${heartbeat_interval}"
                done
              ) &
              heartbeat_pid=$!

              finish_heartbeat() {
                kill "${heartbeat_pid}" 2>/dev/null || true
                wait "${heartbeat_pid}" 2>/dev/null || true
              }

              trap finish_heartbeat EXIT

              sh .github/scripts/deploy.sh
              exit_code=$?

              if [ "${exit_code}" -eq 0 ]; then
                result_status=succeeded
              else
                result_status=failed
              fi

              state_tmp_file="${state_file}.$$"
              if ! DEPLOY_STATUS="${result_status}" \
                DEPLOY_EXIT_CODE="${exit_code}" \
                DEPLOY_METADATA_TMP_FILE="${metadata_tmp_file}" \
                DEPLOY_CHECKS_TMP_FILE="${checks_tmp_file}" \
                DEPLOY_STATE_TMP_FILE="${state_tmp_file}" \
                python3 <<'PY'
              import json
              import os
              import sys

              metadata_file = os.environ["DEPLOY_METADATA_TMP_FILE"]
              checks_file = os.environ["DEPLOY_CHECKS_TMP_FILE"]
              payload = {}
              if os.path.exists(metadata_file):
                  with open(metadata_file, encoding="utf-8") as file:
                      parsed = json.load(file)
                  if isinstance(parsed, dict):
                      payload.update(parsed)
                  else:
                      print(f"Ignoring non-object deploy metadata in {metadata_file}", file=sys.stderr)

              if os.path.exists(checks_file):
                  with open(checks_file, encoding="utf-8") as file:
                      checks = json.load(file)
                  if isinstance(checks, dict):
                      payload["checks"] = checks
                  else:
                      print(f"Ignoring non-object deploy checks in {checks_file}", file=sys.stderr)

              payload["status"] = os.environ["DEPLOY_STATUS"]
              payload["exit_code"] = int(os.environ["DEPLOY_EXIT_CODE"])

              with open(os.environ["DEPLOY_STATE_TMP_FILE"], "w", encoding="utf-8") as file:
                  json.dump(payload, file, sort_keys=True)
                  file.write("\n")
              PY
              then
                printf '{"exit_code":%s,"status":"%s"}\n' "${exit_code}" "${result_status}" > "${state_tmp_file}"
              fi
              mv "${state_tmp_file}" "${state_file}"

              exit "${exit_code}"
          volumeMounts:
            - name: postfix-state
              mountPath: /var/run/heavyedge
          env:
            - name: JOB_NAME
              value: "${JOB_NAME}"
            - name: GIT_SHA
              value: "${GIT_SHA}"
            - name: GITHUB_REPOSITORY
              value: "${GITHUB_REPOSITORY}"
            - name: GITHUB_RUN_URL
              value: "https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
            - name: TAG_NAME
              value: "${RELEASE_TAG_NAME}"
            - name: EVENT_NAME
              value: "${EVENT_NAME}"
            - name: HEAVYEDGE_TEST_MODE
              value: "${HEAVYEDGE_TEST_MODE}"
            - name: UPLOAD_TO_HUGGINGFACE
              value: "${UPLOAD_TO_HUGGINGFACE}"
            - name: PUSH_DOC
              value: "${PUSH_DOC}"
            - name: DEPLOY_STATE_FILE
              value: "/var/run/heavyedge/deploy-state.json"
            - name: DEPLOY_METADATA_TMP_FILE
              value: "/tmp/deploy-metadata.json"
            - name: DEPLOY_CHECKS_TMP_FILE
              value: "/tmp/deploy-checks.json"
            - name: GIT_AUTHOR_NAME
              value: "${GIT_AUTHOR_NAME}"
            - name: GIT_AUTHOR_EMAIL
              value: "${GIT_AUTHOR_EMAIL}"
            - name: HUGGINGFACE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: huggingface-token
                  key: HUGGINGFACE_TOKEN
                  optional: true
        - name: watch-build
          image: curlimages/curl:8.21.0
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -eu

              state_dir="${HEAVYEDGE_STATE_DIR:-/var/run/heavyedge}"
              state_file="${DEPLOY_STATE_FILE:-${state_dir}/deploy-state.json}"
              heartbeat_file="${DEPLOY_BUILD_HEARTBEAT_FILE:-${state_dir}/build-heartbeat}"
              poll_seconds="${DEPLOY_BUILD_WATCH_POLL_SECONDS:-5}"
              initial_timeout_seconds="${DEPLOY_BUILD_INITIAL_TIMEOUT_SECONDS:-600}"
              heartbeat_timeout_seconds="${DEPLOY_BUILD_HEARTBEAT_TIMEOUT_SECONDS:-120}"
              started_at="$(date +%s)"

              write_failed_state() {
                mkdir -p "$(dirname "${state_file}")"
                if [ -f "${state_file}" ]; then
                  return 0
                fi

                state_tmp_file="${state_file}.watch-build.$$"
                cat > "${state_tmp_file}" <<'JSON'
              {"checks":{"gpu_build":{"name":"GPU build","status":"failed","summary":"Build container stopped before publishing deploy state."},"upload_doc":{"name":"Upload document","status":"failed","summary":"Build container stopped before publishing deploy state."},"upload_model":{"name":"Upload model","status":"failed","summary":"Build container stopped before publishing deploy state."}},"exit_code":255,"failure_reason":"build container stopped before publishing deploy state","status":"failed"}
              JSON
                if [ ! -f "${state_file}" ]; then
                  mv "${state_tmp_file}" "${state_file}"
                else
                  rm -f "${state_tmp_file}"
                fi
              }

              while [ ! -f "${state_file}" ]; do
                now="$(date +%s)"

                if [ -f "${heartbeat_file}" ]; then
                  heartbeat_at="$(cat "${heartbeat_file}" 2>/dev/null || true)"
                  case "${heartbeat_at}" in
                    ''|*[!0-9]*)
                      heartbeat_at=0
                      ;;
                  esac

                  if [ $((now - heartbeat_at)) -ge "${heartbeat_timeout_seconds}" ]; then
                    echo "Build heartbeat is stale; publishing failed deploy state."
                    write_failed_state
                    exit 0
                  fi
                elif [ $((now - started_at)) -ge "${initial_timeout_seconds}" ]; then
                  echo "Build heartbeat was not created; publishing failed deploy state."
                  write_failed_state
                  exit 0
                fi

                sleep "${poll_seconds}"
              done
          volumeMounts:
            - name: postfix-state
              mountPath: /var/run/heavyedge
        - name: image-cleanup
          image: curlimages/curl:8.21.0
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -eu

              state_dir="${HEAVYEDGE_STATE_DIR:-/var/run/heavyedge}"
              state_file="${DEPLOY_STATE_FILE:-${state_dir}/deploy-state.json}"

              while [ ! -f "${state_file}" ]; do
                sleep 5
              done

              printf '%s' "${DELETE_IMAGE_SCRIPT_BASE64:?missing delete image script}" | base64 -d > /tmp/delete-image.sh
              sh /tmp/delete-image.sh
          volumeMounts:
            - name: postfix-state
              mountPath: /var/run/heavyedge
          env:
            - name: DELETE_IMAGE_SCRIPT_BASE64
              value: "${DELETE_IMAGE_SCRIPT_BASE64}"
            - name: DOCKER_REGISTRY
              value: "${DOCKER_REGISTRY}"
            - name: DOCKER_REPOSITORY
              value: "${DOCKER_USERNAME}/heavyedge/feature-models"
            - name: DOCKER_TAG
              value: "${DOCKER_TAG}"
            - name: TEMP_DOCKER_TAG
              value: "${TEMP_DOCKER_TAG}"
            - name: DOCKER_USERNAME
              valueFrom:
                secretKeyRef:
                  name: docker-registry-cleanup
                  key: DOCKER_USERNAME
                  optional: true
            - name: DOCKER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: docker-registry-cleanup
                  key: DOCKER_PASSWORD
                  optional: true
        - name: email-notify
          image: curlimages/curl:8.21.0
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
          securityContext:
            runAsUser: 0
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -eu
              set -f

              state_dir="${HEAVYEDGE_STATE_DIR:-/var/run/heavyedge}"
              state_file="${DEPLOY_STATE_FILE:-${state_dir}/deploy-state.json}"
              notify_done_file="${NOTIFY_DEPLOY_DONE_FILE:-${state_dir}/notify-deploy-complete}"

              finish_notify() {
                mkdir -p "$(dirname "${notify_done_file}")"
                touch "${notify_done_file}"
              }

              trap finish_notify EXIT

              if [ -z "${SMTP_NOTIFY_TO:-}" ]; then
                echo "SMTP_NOTIFY_TO is empty; disabling deploy email notification sidecar."
                exit 0
              fi

              build_message() {
                printf 'From: %s\r\n' "${mail_from}"
                printf 'To: %s\r\n' "${SMTP_NOTIFY_TO}"
                printf 'Subject: [%s] %s\r\n' "${subject_status}" "${JOB_NAME:-heavyedge-feature-models}"
                printf 'Content-Type: text/plain; charset=UTF-8\r\n'
                printf 'Content-Transfer-Encoding: 8bit\r\n'
                printf '\r\n'
                printf 'heavyedge-feature-models %s.\r\n' "${subject_status}"
                printf '\r\n'
                printf 'Job: %s\r\n' "${JOB_NAME:-heavyedge-feature-models}"
                printf 'Repository: %s\r\n' "${GITHUB_REPOSITORY:-unknown repository}"
                printf 'Git SHA: %s\r\n' "${GIT_SHA:-unknown}"
                printf 'Event: %s\r\n' "${EVENT_NAME:-unknown}"
                if [ -n "${TAG_NAME:-}" ]; then
                  printf 'Tag: %s\r\n' "${TAG_NAME}"
                fi
                if [ -n "${GITHUB_RUN_URL:-}" ]; then
                  printf 'GitHub Actions run: %s\r\n' "${GITHUB_RUN_URL}"
                fi
                printf '\r\n'
                printf 'HEAVYEDGE_TEST_MODE=%s\r\n' "${HEAVYEDGE_TEST_MODE:-}"
                printf 'UPLOAD_TO_HUGGINGFACE=%s\r\n' "${UPLOAD_TO_HUGGINGFACE:-}"
                printf 'PUSH_DOC=%s\r\n' "${PUSH_DOC:-}"
              }

              send_mail() {
                subject_status="$1"

                if [ -z "${SMTP_NOTIFY_TO:-}" ]; then
                  echo "SMTP_NOTIFY_TO is empty; skipping ${subject_status} notification."
                  return 0
                fi

                mail_from="${SMTP_MAIL_FROM:-heavyedge-feature-models@localhost}"
                smtp_host="${SMTP_HOST:-127.0.0.1}"
                smtp_port="${SMTP_PORT:-587}"
                retries="${SMTP_NOTIFY_RETRIES:-12}"
                delay="${SMTP_NOTIFY_RETRY_DELAY:-5}"

                set -- \
                  --fail \
                  --show-error \
                  --silent \
                  --connect-timeout 10 \
                  --max-time 30 \
                  --url "smtp://${smtp_host}:${smtp_port}" \
                  --mail-from "${mail_from}" \
                  --upload-file -

                rcpt_count=0
                old_ifs="${IFS}"
                IFS=', '
                for recipient in ${SMTP_NOTIFY_TO}; do
                  if [ -n "${recipient}" ]; then
                    set -- "$@" --mail-rcpt "${recipient}"
                    rcpt_count=$((rcpt_count + 1))
                  fi
                done
                IFS="${old_ifs}"

                if [ "${rcpt_count}" -eq 0 ]; then
                  echo "SMTP_NOTIFY_TO has no recipients; skipping ${subject_status} notification."
                  return 0
                fi

                attempt=1
                while [ "${attempt}" -le "${retries}" ]; do
                  if build_message | curl "$@"; then
                    echo "Sent ${subject_status} notification to ${SMTP_NOTIFY_TO}."
                    return 0
                  fi

                  echo "Notification attempt ${attempt}/${retries} failed." >&2
                  if [ "${attempt}" -lt "${retries}" ]; then
                    sleep "${delay}"
                  fi
                  attempt=$((attempt + 1))
                done

                echo "Failed to send ${subject_status} notification." >&2
                return 1
              }

              send_mail started || true

              while [ ! -f "${state_file}" ]; do
                sleep 5
              done

              status="$(sed -n 's/.*"status"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p' "${state_file}" | head -n 1)"

              case "${status:-}" in
                succeeded|failed)
                  send_mail "${status}" || true
                  ;;
                *)
                  echo "Unknown deploy result status '${status:-}'; sending failure notification." >&2
                  send_mail failed || true
                  ;;
              esac
          volumeMounts:
            - name: postfix-state
              mountPath: /var/run/heavyedge
          env:
            - name: JOB_NAME
              value: "${JOB_NAME}"
            - name: GIT_SHA
              value: "${GIT_SHA}"
            - name: GITHUB_REPOSITORY
              value: "${GITHUB_REPOSITORY}"
            - name: GITHUB_RUN_URL
              value: "https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
            - name: TAG_NAME
              value: "${RELEASE_TAG_NAME}"
            - name: EVENT_NAME
              value: "${EVENT_NAME}"
            - name: HEAVYEDGE_TEST_MODE
              value: "${HEAVYEDGE_TEST_MODE}"
            - name: UPLOAD_TO_HUGGINGFACE
              value: "${UPLOAD_TO_HUGGINGFACE}"
            - name: PUSH_DOC
              value: "${PUSH_DOC}"
            - name: SMTP_HOST
              value: "127.0.0.1"
            - name: SMTP_PORT
              value: "587"
            - name: SMTP_NOTIFY_TO
              value: "${SMTP_NOTIFY_TO}"
            - name: SMTP_MAIL_FROM
              valueFrom:
                secretKeyRef:
                  name: gmail-smtp-relay
                  key: GMAIL_USERNAME
        - name: github-app-token
          image: ruby:3.4.10-alpine3.24
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -eu
              set -f
              umask 077

              state_dir="${HEAVYEDGE_STATE_DIR:-/var/run/heavyedge}"
              token_file="${GITHUB_APP_TOKEN_FILE:-${state_dir}/github-app-token}"
              token_metadata_file="${GITHUB_APP_TOKEN_METADATA_FILE:-${state_dir}/github-app-token.json}"
              token_error_file="${GITHUB_APP_TOKEN_ERROR_FILE:-${state_dir}/github-app-token-error}"
              token_done_file="${GITHUB_APP_TOKEN_DONE_FILE:-${state_dir}/github-app-token-complete}"
              refresh_interval="${GITHUB_APP_TOKEN_REFRESH_INTERVAL_SECONDS:-2700}"

              mkdir -p "$(dirname "${token_file}")"

              missing=""
              if [ -z "${GH_APP_ID:-}" ]; then
                missing="${missing} GH_APP_ID"
              fi
              if [ -z "${GH_APP_PRIVATE_KEY:-}" ]; then
                missing="${missing} GH_APP_PRIVATE_KEY"
              fi
              if [ -z "${GH_APP_INSTALLATION_ID:-}" ] && [ -z "${GITHUB_REPOSITORY:-}" ]; then
                missing="${missing} GH_APP_INSTALLATION_ID-or-GITHUB_REPOSITORY"
              fi

              if [ -n "${missing}" ]; then
                echo "GitHub App token environment is incomplete (missing:${missing}); token sidecar disabled." | tee "${token_error_file}" >&2
                exit 0
              fi

              refresh_github_app_token() {
                GITHUB_APP_TOKEN_FILE="${token_file}" \
                  GITHUB_APP_TOKEN_METADATA_FILE="${token_metadata_file}" \
                  GITHUB_APP_TOKEN_ERROR_FILE="${token_error_file}" \
                  ruby <<'RUBY'
              require "base64"
              require "fileutils"
              require "json"
              require "net/http"
              require "openssl"
              require "time"
              require "uri"

              def base64_url(data)
                Base64.urlsafe_encode64(data, padding: false)
              end

              def jwt_segment(payload)
                base64_url(JSON.generate(payload))
              end

              def github_request(method, url, token, payload = nil, allow_failure: false)
                uri = URI(url)
                request_class = {
                  "GET" => Net::HTTP::Get,
                  "POST" => Net::HTTP::Post,
                  "PATCH" => Net::HTTP::Patch,
                }.fetch(method)
                request = request_class.new(uri)
                request["Accept"] = "application/vnd.github+json"
                request["Authorization"] = "Bearer #{token}"
                request["X-GitHub-Api-Version"] = "2022-11-28"
                if payload
                  request["Content-Type"] = "application/json"
                  request.body = JSON.generate(payload)
                end

                response = Net::HTTP.start(uri.host, uri.port, use_ssl: uri.scheme == "https") do |http|
                  http.open_timeout = 10
                  http.read_timeout = 30
                  http.request(request)
                end

                status = response.code.to_i
                if status < 200 || status >= 300
                  return [status, response.body.to_s] if allow_failure

                  warn "GitHub API request failed: #{method} #{url} returned HTTP #{status}"
                  warn "Response body:"
                  warn response.body.to_s
                  if payload
                    warn "Request payload:"
                    warn JSON.generate(payload)
                  end
                  raise "GitHub API request failed"
                end

                body = response.body.to_s
                parsed_body = body.empty? ? nil : JSON.parse(body)
                [status, parsed_body]
              end

              def atomic_write(path, body)
                FileUtils.mkdir_p(File.dirname(path))
                tmp_path = "#{path}.#{Process.pid}.tmp"
                File.write(tmp_path, body)
                File.rename(tmp_path, path)
              end

              def write_error(path, error)
                atomic_write(path, "#{Time.now.utc.iso8601} #{error.class}: #{error.message}\n")
              rescue StandardError => write_error
                warn "Failed to write token error file #{path}: #{write_error.class}: #{write_error.message}"
              end

              begin
                owner = nil
                repo = nil
                if ENV["GITHUB_REPOSITORY"].to_s.include?("/")
                  owner, repo = ENV.fetch("GITHUB_REPOSITORY").split("/", 2)
                end

                private_key = OpenSSL::PKey.read(ENV.fetch("GH_APP_PRIVATE_KEY").gsub("\\n", "\n"))
                now = Time.now.to_i
                signing_input = [
                  jwt_segment({ "alg" => "RS256", "typ" => "JWT" }),
                  jwt_segment({ "iat" => now - 60, "exp" => now + 540, "iss" => ENV.fetch("GH_APP_ID") }),
                ].join(".")
                jwt = "#{signing_input}.#{base64_url(private_key.sign(OpenSSL::Digest::SHA256.new, signing_input))}"

                installation_id = ENV["GH_APP_INSTALLATION_ID"].to_s
                if installation_id.empty?
                  raise "GITHUB_REPOSITORY must be owner/repo when GH_APP_INSTALLATION_ID is not set" if owner.to_s.empty? || repo.to_s.empty?

                  _, installation = github_request("GET", "https://api.github.com/repos/#{owner}/#{repo}/installation", jwt)
                  installation_id = installation.fetch("id")
                end

                permissions_json = ENV["GITHUB_APP_TOKEN_PERMISSIONS"].to_s.strip
                token_payload = nil
                unless permissions_json.empty?
                  token_payload = { "permissions" => JSON.parse(permissions_json) }
                end

                _, token_response = github_request(
                  "POST",
                  "https://api.github.com/app/installations/#{installation_id}/access_tokens",
                  jwt,
                  token_payload,
                )

                token = token_response.fetch("token")
                expires_at = token_response["expires_at"].to_s
                metadata = {
                  "installation_id" => installation_id,
                  "repository" => ENV["GITHUB_REPOSITORY"].to_s,
                  "expires_at" => expires_at,
                  "permissions" => permissions_json.empty? ? "installation-default" : JSON.parse(permissions_json),
                  "updated_at" => Time.now.utc.iso8601,
                }

                atomic_write(ENV.fetch("GITHUB_APP_TOKEN_FILE"), "#{token}\n")
                atomic_write(ENV.fetch("GITHUB_APP_TOKEN_METADATA_FILE"), "#{JSON.pretty_generate(metadata)}\n")
                FileUtils.rm_f(ENV.fetch("GITHUB_APP_TOKEN_ERROR_FILE"))
                puts "Refreshed GitHub App installation token; expires_at=#{expires_at.empty? ? 'unknown' : expires_at}."
              rescue StandardError => error
                write_error(ENV.fetch("GITHUB_APP_TOKEN_ERROR_FILE"), error)
                warn "Failed to refresh GitHub App installation token: #{error.class}: #{error.message}"
                exit 1
              end
              RUBY
              }

              while :; do
                refresh_github_app_token || true

                elapsed=0
                while [ "${elapsed}" -lt "${refresh_interval}" ]; do
                  if [ -f "${token_done_file}" ]; then
                    exit 0
                  fi
                  sleep 5
                  elapsed=$((elapsed + 5))
                done
              done
          volumeMounts:
            - name: postfix-state
              mountPath: /var/run/heavyedge
          env:
            - name: GITHUB_REPOSITORY
              value: "${GITHUB_REPOSITORY}"
            - name: GITHUB_APP_TOKEN_PERMISSIONS
              value: '{"contents":"write","checks":"write"}'
            - name: GH_APP_ID
              valueFrom:
                secretKeyRef:
                  name: github-app
                  key: GH_APP_ID
                  optional: true
            - name: GH_APP_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: github-app
                  key: GH_APP_PRIVATE_KEY
                  optional: true
            - name: GH_APP_INSTALLATION_ID
              valueFrom:
                secretKeyRef:
                  name: github-app
                  key: GH_APP_INSTALLATION_ID
                  optional: true
        - name: github-check
          image: ruby:3.4.10-alpine3.24
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -eu
              set -f

              state_dir="${HEAVYEDGE_STATE_DIR:-/var/run/heavyedge}"
              state_file="${DEPLOY_STATE_FILE:-${state_dir}/deploy-state.json}"
              token_file="${GITHUB_APP_TOKEN_FILE:-${state_dir}/github-app-token}"
              token_error_file="${GITHUB_APP_TOKEN_ERROR_FILE:-${state_dir}/github-app-token-error}"
              token_done_file="${GITHUB_APP_TOKEN_DONE_FILE:-${state_dir}/github-app-token-complete}"
              token_wait_seconds="${GITHUB_APP_TOKEN_WAIT_SECONDS:-120}"

              finish_check_run() {
                mkdir -p "$(dirname "${token_done_file}")"
                touch "${token_done_file}"
              }

              trap finish_check_run EXIT

              wait_for_github_token() {
                waited=0
                while [ ! -s "${token_file}" ]; do
                  if [ -s "${token_error_file}" ]; then
                    echo "GitHub App token sidecar reported an error; skipping check-run update." >&2
                    cat "${token_error_file}" >&2
                    return 1
                  fi
                  if [ "${waited}" -ge "${token_wait_seconds}" ]; then
                    echo "Timed out waiting for GitHub App token at ${token_file}; skipping check-run update." >&2
                    return 1
                  fi
                  sleep 2
                  waited=$((waited + 2))
                done
              }

              update_github_checks() {
                if ! wait_for_github_token; then
                  return 0
                fi

                DEPLOY_STATE_FILE="${state_file}" \
                  GITHUB_APP_TOKEN_FILE="${token_file}" \
                  ruby <<'RUBY'
              require "json"
              require "net/http"
              require "time"
              require "uri"

              token_file = ENV.fetch("GITHUB_APP_TOKEN_FILE")
              state_file = ENV.fetch("DEPLOY_STATE_FILE")

              missing = %w[GITHUB_REPOSITORY GIT_SHA].select do |name|
                ENV[name].to_s.empty?
              end

              unless missing.empty?
                warn "GitHub check-run environment is incomplete (missing #{missing.join(', ')}); skipping check-run updates."
                exit 0
              end

              token = File.read(token_file).strip
              if token.empty?
                warn "GitHub App token file #{token_file} is empty; skipping check-run updates."
                exit 0
              end

              owner, repo = ENV.fetch("GITHUB_REPOSITORY").split("/", 2)
              if owner.to_s.empty? || repo.to_s.empty?
                warn "Invalid GITHUB_REPOSITORY=#{ENV.fetch('GITHUB_REPOSITORY').inspect}; skipping check-run updates."
                exit 0
              end

              def github_request(method, url, token, payload = nil, allow_failure: false)
                uri = URI(url)
                request_class = {
                  "GET" => Net::HTTP::Get,
                  "POST" => Net::HTTP::Post,
                  "PATCH" => Net::HTTP::Patch,
                }.fetch(method)
                request = request_class.new(uri)
                request["Accept"] = "application/vnd.github+json"
                request["Authorization"] = "Bearer #{token}"
                request["X-GitHub-Api-Version"] = "2022-11-28"
                if payload
                  request["Content-Type"] = "application/json"
                  request.body = JSON.generate(payload)
                end

                response = Net::HTTP.start(uri.host, uri.port, use_ssl: uri.scheme == "https") do |http|
                  http.open_timeout = 10
                  http.read_timeout = 30
                  http.request(request)
                end

                status = response.code.to_i
                if status < 200 || status >= 300
                  return [status, response.body.to_s] if allow_failure

                  warn "GitHub API request failed: #{method} #{url} returned HTTP #{status}"
                  warn "Response body:"
                  warn response.body.to_s
                  if payload
                    warn "Request payload:"
                    warn JSON.generate(payload)
                  end
                  raise "GitHub API request failed"
                end

                body = response.body.to_s
                parsed_body = body.empty? ? nil : JSON.parse(body)
                [status, parsed_body]
              end

              def default_summary(check_name, status)
                case status
                when "succeeded"
                  "#{check_name} succeeded."
                when "failed"
                  "#{check_name} failed."
                when "skipped"
                  "#{check_name} skipped."
                else
                  "#{check_name} completed."
                end
              end

              def normalize_status(raw_status, fallback_status)
                case raw_status.to_s
                when "succeeded", "failed", "skipped"
                  raw_status.to_s
                else
                  fallback_status
                end
              end

              def check_payload(status, payload_kind, check_name, git_sha, job_name, github_run_url, summary)
                conclusion = {
                  "succeeded" => "success",
                  "failed" => "failure",
                  "skipped" => "skipped",
                }.fetch(status, "failure")

                payload = {
                  "details_url" => github_run_url.to_s,
                  "status" => "completed",
                  "conclusion" => conclusion,
                  "completed_at" => Time.now.utc.iso8601,
                  "output" => {
                    "title" => check_name,
                    "summary" => summary.to_s.empty? ? default_summary(check_name, status) : summary.to_s,
                  },
                }

                if payload_kind == "create"
                  payload["name"] = check_name
                  payload["head_sha"] = git_sha
                  payload["external_id"] = job_name.to_s
                end

                payload
              end

              begin
                deploy_state = JSON.parse(File.read(state_file))
              rescue StandardError => error
                warn "Failed to read deploy state #{state_file}: #{error.class}: #{error.message}"
                deploy_state = {}
              end
              deploy_state = {} unless deploy_state.is_a?(Hash)
              check_states = deploy_state["checks"].is_a?(Hash) ? deploy_state["checks"] : {}
              fallback_status = deploy_state["status"].to_s == "succeeded" ? "succeeded" : "failed"

              git_sha = ENV.fetch("GIT_SHA")
              job_name = ENV["JOB_NAME"].to_s
              github_run_url = ENV["GITHUB_RUN_URL"].to_s
              api_url = "https://api.github.com/repos/#{owner}/#{repo}/check-runs"

              checks = [
                ["gpu_build", "GPU build", ENV["GITHUB_GPU_BUILD_CHECK_RUN_ID"].to_s],
                ["upload_model", "Upload model", ENV["GITHUB_UPLOAD_MODEL_CHECK_RUN_ID"].to_s],
                ["upload_doc", "Upload document", ENV["GITHUB_UPLOAD_DOC_CHECK_RUN_ID"].to_s],
              ]

              checks.each do |key, default_name, check_id|
                check_state = check_states[key].is_a?(Hash) ? check_states[key] : {}
                check_name = check_state["name"].to_s.empty? ? default_name : check_state["name"].to_s
                check_status = normalize_status(check_state["status"], fallback_status)
                summary = check_state["summary"].to_s

                if !check_id.empty?
                  payload = check_payload(check_status, "update", check_name, git_sha, job_name, github_run_url, summary)
                  github_request("PATCH", "#{api_url}/#{check_id}", token, payload)
                else
                  payload = check_payload(check_status, "create", check_name, git_sha, job_name, github_run_url, summary)
                  github_request("POST", api_url, token, payload)
                end
              end
              RUBY
              }

              while [ ! -f "${state_file}" ]; do
                sleep 5
              done

              update_github_checks || true
          volumeMounts:
            - name: postfix-state
              mountPath: /var/run/heavyedge
          env:
            - name: JOB_NAME
              value: "${JOB_NAME}"
            - name: GIT_SHA
              value: "${GIT_SHA}"
            - name: GITHUB_REPOSITORY
              value: "${GITHUB_REPOSITORY}"
            - name: GITHUB_RUN_URL
              value: "https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
            - name: GITHUB_GPU_BUILD_CHECK_RUN_ID
              value: "${GITHUB_GPU_BUILD_CHECK_RUN_ID}"
            - name: GITHUB_UPLOAD_MODEL_CHECK_RUN_ID
              value: "${GITHUB_UPLOAD_MODEL_CHECK_RUN_ID}"
            - name: GITHUB_UPLOAD_DOC_CHECK_RUN_ID
              value: "${GITHUB_UPLOAD_DOC_CHECK_RUN_ID}"
        - name: dispatch-workflow
          image: ruby:3.4.10-alpine3.24
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -eu
              set -f

              state_dir="${HEAVYEDGE_STATE_DIR:-/var/run/heavyedge}"
              state_file="${DEPLOY_STATE_FILE:-${state_dir}/deploy-state.json}"
              token_file="${GITHUB_APP_TOKEN_FILE:-${state_dir}/github-app-token}"
              token_error_file="${GITHUB_APP_TOKEN_ERROR_FILE:-${state_dir}/github-app-token-error}"
              token_wait_seconds="${GITHUB_APP_TOKEN_WAIT_SECONDS:-120}"

              wait_for_github_token() {
                waited=0
                while [ ! -s "${token_file}" ]; do
                  if [ -s "${token_error_file}" ]; then
                    echo "GitHub App token sidecar reported an error; skipping image workflow dispatch." >&2
                    cat "${token_error_file}" >&2
                    return 1
                  fi
                  if [ "${waited}" -ge "${token_wait_seconds}" ]; then
                    echo "Timed out waiting for GitHub App token at ${token_file}; skipping image workflow dispatch." >&2
                    return 1
                  fi
                  sleep 2
                  waited=$((waited + 2))
                done
              }

              dispatch_image_workflow() {
                if ! wait_for_github_token; then
                  return 0
                fi

                GITHUB_APP_TOKEN_FILE="${token_file}" \
                  DEPLOY_STATE_FILE="${state_file}" \
                  ruby <<'RUBY'
              require "json"
              require "net/http"
              require "time"
              require "uri"

              missing = %w[GITHUB_REPOSITORY GIT_SHA].select do |name|
                ENV[name].to_s.empty?
              end

              unless missing.empty?
                warn "Image workflow dispatch environment is incomplete (missing #{missing.join(', ')}); skipping dispatch."
                exit 0
              end

              token = File.read(ENV.fetch("GITHUB_APP_TOKEN_FILE")).strip
              if token.empty?
                warn "GitHub App token file is empty; skipping image workflow dispatch."
                exit 0
              end

              state_file = ENV.fetch("DEPLOY_STATE_FILE")
              deploy_state = File.size?(state_file) ? JSON.parse(File.read(state_file)) : {}
              deploy_state = {} unless deploy_state.is_a?(Hash)

              owner, repo = ENV.fetch("GITHUB_REPOSITORY").split("/", 2)
              if owner.to_s.empty? || repo.to_s.empty?
                warn "Invalid GITHUB_REPOSITORY=#{ENV.fetch('GITHUB_REPOSITORY').inspect}; skipping image workflow dispatch."
                exit 0
              end

              payload = {
                "event_type" => ENV["IMAGE_WORKFLOW_EVENT_TYPE"].to_s.empty? ? "build image" : ENV["IMAGE_WORKFLOW_EVENT_TYPE"],
                "client_payload" => {
                  "model" => {
                    "repo" => deploy_state["model_repo"].to_s,
                    "revision" => deploy_state["model_revision"].to_s,
                  },
                },
              }

              uri = URI("https://api.github.com/repos/#{owner}/#{repo}/dispatches")
              request = Net::HTTP::Post.new(uri)
              request["Accept"] = "application/vnd.github+json"
              request["Authorization"] = "Bearer #{token}"
              request["Content-Type"] = "application/json"
              request["X-GitHub-Api-Version"] = "2022-11-28"
              request.body = JSON.generate(payload)

              response = Net::HTTP.start(uri.host, uri.port, use_ssl: true) do |http|
                http.open_timeout = 10
                http.read_timeout = 30
                http.request(request)
              end

              status = response.code.to_i
              if status < 200 || status >= 300
                warn "Image workflow dispatch failed with HTTP #{status}."
                warn response.body.to_s
                exit 1
              end

              puts "Dispatched image workflow event #{payload.fetch('event_type')} for #{ENV.fetch('GIT_SHA')}."
              RUBY
              }

              while [ ! -f "${state_file}" ]; do
                sleep 5
              done

              dispatch_image_workflow || true
          volumeMounts:
            - name: postfix-state
              mountPath: /var/run/heavyedge
          env:
            - name: JOB_NAME
              value: "${JOB_NAME}"
            - name: GIT_SHA
              value: "${GIT_SHA}"
            - name: GITHUB_REPOSITORY
              value: "${GITHUB_REPOSITORY}"
            - name: GITHUB_RUN_URL
              value: "https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
            - name: TAG_NAME
              value: "${RELEASE_TAG_NAME}"
            - name: EVENT_NAME
              value: "${EVENT_NAME}"
            - name: DOCKER_TAG
              value: "${DOCKER_TAG}"
            - name: DEPLOY_STATE_FILE
              value: "/var/run/heavyedge/deploy-state.json"
            - name: IMAGE_WORKFLOW_EVENT_TYPE
              value: "build image"
        - name: postfix
          image: boky/postfix:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
          ports:
            - name: smtp
              containerPort: 587
          command: ["/bin/sh", "-c"]
          args:
            - |
              if [ -z "${SMTP_NOTIFY_TO:-}" ]; then
                echo "SMTP_NOTIFY_TO is empty; disabling postfix sidecar."
                exit 0
              fi

              /scripts/run.sh &
              postfix_pid=$!
              while [ ! -f /var/run/heavyedge/notify-deploy-complete ]; do
                if ! kill -0 "${postfix_pid}" 2>/dev/null; then
                  wait "${postfix_pid}"
                  exit $?
                fi
                sleep 5
              done
              postfix stop || true
              kill "${postfix_pid}" 2>/dev/null || true
              wait "${postfix_pid}" 2>/dev/null || true
          env:
            - name: SMTP_NOTIFY_TO
              value: "${SMTP_NOTIFY_TO}"
            - name: RELAYHOST
              value: "[smtp.gmail.com]:587"
            - name: RELAYHOST_USERNAME
              valueFrom:
                secretKeyRef:
                  name: gmail-smtp-relay
                  key: GMAIL_USERNAME
            - name: RELAYHOST_PASSWORD_FILE
              value: "/run/secrets/gmail/GMAIL_APP_PASSWORD"
            - name: POSTFIX_smtp_tls_security_level
              value: "encrypt"
            - name: POSTFIX_mynetworks
              value: "127.0.0.0/8 [::1]/128"
            - name: ALLOW_EMPTY_SENDER_DOMAINS
              value: "true"
            - name: LOG_FORMAT
              value: "json"
          volumeMounts:
            - name: postfix-state
              mountPath: /var/run/heavyedge
            - name: gmail-smtp-relay
              mountPath: /run/secrets/gmail
              readOnly: true
